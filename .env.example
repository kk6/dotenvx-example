# GitHub Personal Access Token (optional — unauthenticated: 60 req/hour)
GITHUB_TOKEN=

# PostgreSQL connection URL (asyncpg driver)
DATABASE_URL=postgresql+asyncpg://appuser:CHANGE_ME@db:5432/appdb

# PostgreSQL password (must match DATABASE_URL)
POSTGRES_PASSWORD=CHANGE_ME
